# Copier ce fichier en .env et remplir les valeurs.
# Le fichier .env ne doit JAMAIS être versionné (il est dans .gitignore).

# Base de données MySQL
DB_USER=sae204_xx_user
DB_PASSWORD=votre_mot_de_passe
DB_HOST=mysql-sae204.alwaysdata.net
DB_NAME=sae204_xx_bd

# Clé secrète Flask (sessions) : une longue chaîne aléatoire
SECRET_KEY=a-remplacer-par-une-cle-aleatoire

# Connexion à l'espace administrateur (/admin)
ADMIN_USER=admin
ADMIN_PASSWORD=votre_mot_de_passe_admin

# Sous-chemin de déploiement : vide en local, ex. /sae201_b1 en production
APP_BASE_URL=
